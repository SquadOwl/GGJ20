using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JoystikLogic : MonoBehaviour
{
    Joystick obj;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (obj.Horizontal == 1)
        {

        }
        if (obj.Horizontal == -1)
        {

        }
        if (obj.Vertical == 1)
        {

        }
        if (obj.Horizontal == -1)
        {

        }

    }
}
